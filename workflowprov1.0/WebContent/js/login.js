
/*$(document).ready(function(){
    $('#seconnecter').click(function (e) { 
        e.preventDefault();

        let username = $('#username').text();
        let password = $('#password').text();

        alert("Le nom d'utilisateur est "+username+ " et le mot de passe est "+password)

        if(username.length <= 0 || password.length <= 0)
        {
            alert ("Le champs nom d'utilisateur ou mot de passe ne doit pas être vide !")
        }
        else
        {
            e.stopPropagation();
        }

        

    });
});

*/