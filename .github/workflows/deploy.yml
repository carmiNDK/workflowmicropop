name: Build and Deploy JavaEE WAR to EC2

on:
  workflow_dispatch: {}
  schedule:
    # 23:55 UTC = 00:55 à Kinshasa
    - cron: '47 23 * * 1'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up JDK 8
        uses: actions/setup-java@v4
        with:
          java-version: '8'
          distribution: 'temurin'

      # ÉTAPE 1 : COMPILATION (AVEC FIX POUR LES ACCENTS)
      - name: Build WAR
        run: |
          cd "workflowprov1.0"
          mkdir -p build/WEB-INF/classes
          mkdir -p dist
          
          # Liste des sources
          find src -name "*.java" > sources.txt
          
          # Liste des librairies (Classpath)
          LIBS=$(find . -name "*.jar" | tr '\n' ':')
          
          # COMPILATION AVEC FORCAGE DE L'ENCODAGE
          javac -encoding ISO-8859-1 -d build/WEB-INF/classes -cp "$LIBS" @sources.txt
          
          # Assemblage du contenu Web
          cp -r WebContent/* build/
          
          # Création du fichier WAR
          cd build && jar -cvf ../dist/workflowmicropop.war .
          echo "Compilation terminée avec succès."

      # ÉTAPE 2 : PRÉPARATION DU DOSSIER DE DÉPLOIEMENT
      - name: Prepare Package
        run: |
          rm -rf ./out_package deploydoc.zip
          mkdir -p ./out_package
          
          ROOT_DIR="workflowprov1.0"
          
          # Copie du nouveau WAR
          cp "$ROOT_DIR/dist/workflowmicropop.war" ./out_package/
          
          # Nettoyage et transfert des scripts CodeDeploy
          cat "$ROOT_DIR/deploydoc/deploy.sh" | tr -d '\r' | iconv -t UTF-8//IGNORE > ./out_package/deploy.sh
          cat "$ROOT_DIR/deploydoc/appspec.yml" | tr -d '\r' | iconv -t UTF-8//IGNORE > ./out_package/appspec.yml
          
          chmod +x ./out_package/deploy.sh

      # ÉTAPE 3 : ZIP ET ENVOI VERS AWS
      - name: Create Zip and Deploy
        run: |
          cd ./out_package && zip ../deploydoc.zip appspec.yml deploy.sh workflowmicropop.war
          cd ..
          
          export AWS_ACCESS_KEY_ID=${{ secrets.AWS_ACCESS_KEY }}
          export AWS_SECRET_ACCESS_KEY=${{ secrets.AWS_SECRET_KEY }}
          export AWS_DEFAULT_REGION=eu-west-3
          
          # Envoi sur S3
          aws s3 cp deploydoc.zip s3://micropopdev/deploydoc.zip
          
          # Déclenchement CodeDeploy
          aws deploy create-deployment \
            --application-name micropop-app \
            --deployment-group-name micropop-dg \
            --s3-location bucket=micropopdev,key=deploydoc.zip,bundleType=zip \
            --region eu-west-3 \
            --file-exists-behavior OVERWRITE